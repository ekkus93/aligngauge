[package]
name = "aligngauge-hts"
version = "0.0.0"
description = "Validated BAM input boundary for AlignGauge"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
aligngauge-core = { path = "../aligngauge-core" }
flate2 = "1.1"
rust-htslib = { version = "1.0.1", default-features = false }
sha2 = "0.10"

[dev-dependencies]
aligngauge-testkit = { path = "../aligngauge-testkit" }

[lints]
workspace = true
